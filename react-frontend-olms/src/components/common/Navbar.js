// Navigation bar component placeholder
import React from 'react';

const Navbar = () => {
  return (
    <nav>
      {/* Navigation bar implementation will be added in later tasks */}
    </nav>
  );
};

export default Navbar;