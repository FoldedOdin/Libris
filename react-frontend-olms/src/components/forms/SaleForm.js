// Sale form component placeholder
import React from 'react';

const SaleForm = () => {
  return (
    <form>
      {/* Sale form implementation will be added in later tasks */}
    </form>
  );
};

export default SaleForm;