// Book listing component placeholder
import React from 'react';

const BookList = () => {
  return (
    <div>
      {/* Book list implementation will be added in later tasks */}
    </div>
  );
};

export default BookList;