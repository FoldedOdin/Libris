// User listing component placeholder
import React from 'react';

const UserList = () => {
  return (
    <div>
      {/* User list implementation will be added in later tasks */}
    </div>
  );
};

export default UserList;