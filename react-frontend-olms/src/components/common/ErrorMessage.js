// Error message component placeholder
import React from 'react';

const ErrorMessage = ({ message }) => {
  return (
    <div>
      {/* Error message implementation will be added in later tasks */}
      {message && <p>Error: {message}</p>}
    </div>
  );
};

export default ErrorMessage;